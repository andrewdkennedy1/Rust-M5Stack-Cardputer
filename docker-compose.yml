services:
  builder:
    build: .
    volumes:
      - .:/workspace
      - rust_cache:/cache
    environment:
      - CARGO_TARGET_DIR=/cache/target
      - CARGO_HOME=/cache/cargo

volumes:
  rust_cache:
